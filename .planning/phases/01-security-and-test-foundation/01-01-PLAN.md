---
phase: 01-security-and-test-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.gitignore, .github/workflows/security-and-test-foundation.yml, Backend/Chatify/package.json, Backend/Chatify/package-lock.json, Backend/Chatify/vitest.config.mjs, Backend/Chatify/test/setup/env.mjs, Backend/Chatify/test/setup/mongo.mjs, Backend/Chatify/test/setup/app.mjs, Backend/Chatify/test/fixtures/users.mjs, Backend/Chatify/test/fixtures/chats.mjs, Backend/Chatify/test/fixtures/messages.mjs, Backend/Chatify/test/helpers/authAgent.mjs, Backend/Chatify/test/auth/auth.lifecycle.test.mjs, Backend/Chatify/test/message/message.authorization.test.mjs]
autonomous: true
requirements: [AUTH-01, TEST-01, TEST-04]
user_setup: []
must_haves:
  truths:
    - "Decision coverage: D-01 D-02 D-03 D-04 D-21 D-22 D-23 D-26."
    - `npm test` from `Backend/Chatify` runs Vitest once and exits 0 for baseline passing tests.
    - Backend tests use `Backend/Chatify/app.mjs` with Supertest and do not start `server.mjs`.
    - Baseline HTTP message tests reject unauthenticated users and non-members for representative protected endpoints.
    - CI can commit and run under `.github/workflows/` despite the current `.gitignore`.
  artifacts:
    - `Backend/Chatify/vitest.config.mjs`
    - `Backend/Chatify/test/setup/mongo.mjs`
    - `Backend/Chatify/test/auth/auth.lifecycle.test.mjs`
    - `Backend/Chatify/test/message/message.authorization.test.mjs`
    - `.github/workflows/security-and-test-foundation.yml`
  key_links:
    - `Backend/Chatify/package.json` `test` script points at Vitest.
    - Supertest helpers import the Express app only after test env and memory Mongo are configured.
    - CI uses Node 22 and package lockfiles for backend and frontend installs.
---

<objective>
Create the deterministic backend/security test harness and first regression fence for auth lifecycle and HTTP message authorization.

Purpose: Phase 1 needs a real verification foundation before changing security-sensitive auth, CSRF, reset, and logging behavior.
Output: Backend Vitest/Supertest/memory-Mongo setup, baseline route tests, package scripts, and a GitHub Actions workflow skeleton.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-and-test-foundation/01-SPEC.md
@.planning/phases/01-security-and-test-foundation/01-CONTEXT.md
@.planning/phases/01-security-and-test-foundation/01-RESEARCH.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@Backend/Chatify/app.mjs
@Backend/Chatify/package.json
@Backend/Chatify/Routes/authRouter.mjs
@Backend/Chatify/Routes/messageRouter.mjs
@Backend/Chatify/Models/userModel.mjs
@Backend/Chatify/Models/chatModel.mjs
@Backend/Chatify/Models/messageModel.mjs
@Backend/Chatify/Controller/messageController.mjs
</context>

<decision_traceability>
- D-01: Dedicated `Backend/Chatify/test/` tree.
- D-02: Supertest imports `Backend/Chatify/app.mjs`, not `server.mjs`.
- D-03: Hybrid fixtures use Mongoose factories and public auth API flows.
- D-04: Message authorization coverage stays at HTTP route boundaries.
- D-21: GitHub Actions runs backend tests plus frontend lint/build.
- D-22: CI uses Node 22.
- D-23: Phase keeps the approved three-plan split.
- D-26: Plan verification forbids edits to `Frontend/Chatify/src/pages/chat/chat.tsx`.
</decision_traceability>

<tasks>

<task type="auto">
  <name>Task 1: Install backend test stack and scripts</name>
  <files>Backend/Chatify/package.json, Backend/Chatify/package-lock.json, Backend/Chatify/vitest.config.mjs</files>
  <read_first>Backend/Chatify/package.json, .planning/phases/01-security-and-test-foundation/01-RESEARCH.md</read_first>
  <action>Install `vitest`, `supertest`, and `mongodb-memory-server` as backend devDependencies. Replace the placeholder backend `test` script with `vitest run --config vitest.config.mjs`; add `test:watch` as `vitest --config vitest.config.mjs`. Create `Backend/Chatify/vitest.config.mjs` with `environment: 'node'`, backend test include patterns under `test/**/*.test.mjs`, and setup files that run before tests. Keep this backend-local; do not add a root lockfile.</action>
  <verify>cd Backend/Chatify; npm test -- --help</verify>
  <acceptance_criteria>
    - `Backend/Chatify/package.json` contains `"test": "vitest run --config vitest.config.mjs"`.
    - `Backend/Chatify/package.json` devDependencies include `vitest`, `supertest`, and `mongodb-memory-server`.
    - `Backend/Chatify/vitest.config.mjs` exists and targets the Node environment.
  </acceptance_criteria>
  <done>The backend test command starts Vitest instead of the placeholder failure.</done>
</task>

<task type="auto">
  <name>Task 2: Add isolated test environment and app loader</name>
  <files>Backend/Chatify/test/setup/env.mjs, Backend/Chatify/test/setup/mongo.mjs, Backend/Chatify/test/setup/app.mjs</files>
  <read_first>Backend/Chatify/app.mjs, Backend/Chatify/Config/DBConfig.mjs, Backend/Chatify/server.mjs</read_first>
  <action>Create setup helpers that set deterministic test env before app import: `NODE_ENV=test`, `SECRET_JWT_KEY=test-secret-key`, `EXPIRES_IN=15m`, `PASSWORD_RESET_SECRET=test-password-reset-secret`, `FRONTEND_ORIGIN=http://localhost:5173`, `FRONTEND_ORIGIN_DEV=http://localhost:5173`, and safe OAuth/Brevo placeholders. Start `MongoMemoryServer`, assign its URI to `MONGODB_URL`, connect Mongoose once, clear collections between tests, and disconnect/stop after the suite. Export a lazy `getTestApp()` that dynamically imports `../../app.mjs` only after env and Mongo are ready. Do not import `server.mjs`.</action>
  <verify>cd Backend/Chatify; npm test -- --runInBand</verify>
  <acceptance_criteria>
    - Setup files never read local `.env`.
    - `getTestApp()` imports `Backend/Chatify/app.mjs`, not `server.mjs`.
    - Tests can clear Mongoose collections between cases.
  </acceptance_criteria>
  <done>Backend test setup is deterministic and independent of local services.</done>
</task>

<task type="auto">
  <name>Task 3: Add fixtures and authenticated request helpers</name>
  <files>Backend/Chatify/test/fixtures/users.mjs, Backend/Chatify/test/fixtures/chats.mjs, Backend/Chatify/test/fixtures/messages.mjs, Backend/Chatify/test/helpers/authAgent.mjs</files>
  <read_first>Backend/Chatify/Models/userModel.mjs, Backend/Chatify/Models/chatModel.mjs, Backend/Chatify/Models/messageModel.mjs, Backend/Chatify/Utils/tokenCookieGenerator.mjs</read_first>
  <action>Create Mongoose fixture factories for local users, two-member chats, outsider users, and messages. Create `authAgent.mjs` helpers that use Supertest agents to sign up or log in through public auth routes and preserve cookies. Use generated test emails such as `member-${Date.now()}@example.test`, never real emails. Keep fixtures minimal and aligned to current model required fields.</action>
  <verify>cd Backend/Chatify; npm test</verify>
  <acceptance_criteria>
    - Fixture factories return persisted documents with valid ObjectIds.
    - Auth helpers preserve `accessToken` cookies through Supertest agents.
    - No fixture imports production env or external services.
  </acceptance_criteria>
  <done>Route tests can authenticate users and create chats/messages without repeated setup code.</done>
</task>

<task type="auto">
  <name>Task 4: Cover baseline auth lifecycle behavior</name>
  <files>Backend/Chatify/test/auth/auth.lifecycle.test.mjs</files>
  <read_first>Backend/Chatify/Routes/authRouter.mjs, Backend/Chatify/Controller/authController.mjs, Backend/Chatify/Utils/tokenCookieGenerator.mjs</read_first>
  <action>Add Vitest/Supertest tests for signup, login, logout, `GET /api/auth/is-authenticated`, invalid credentials, and remember-me cookie max-age/options that currently exist. Keep expired-token and refresh-failure assertions for Plan 02 because that plan changes the behavior. Assert cookie presence/clearing through response headers rather than depending on browser behavior.</action>
  <verify>cd Backend/Chatify; npm test -- test/auth/auth.lifecycle.test.mjs</verify>
  <acceptance_criteria>
    - Signup and login set an `accessToken` cookie.
    - Logout clears `accessToken`.
    - Remember-me uses a longer max-age than the normal login cookie.
    - Invalid credentials return a non-2xx status.
  </acceptance_criteria>
  <done>Current auth lifecycle behavior is pinned before security changes.</done>
</task>

<task type="auto">
  <name>Task 5: Cover baseline HTTP message authorization boundaries</name>
  <files>Backend/Chatify/test/message/message.authorization.test.mjs</files>
  <read_first>Backend/Chatify/Routes/messageRouter.mjs, Backend/Chatify/Controller/messageController.mjs, Backend/Chatify/Models/chatModel.mjs, Backend/Chatify/Models/messageModel.mjs</read_first>
  <action>Add tests for representative protected message routes: unauthenticated `POST /api/message/new-message` returns 401, invalid chat id returns 400, outsider/non-member access to `GET /api/message/get-all-messages/:id`, `POST /api/message/new-message`, `PATCH /api/message/:messageId/read`, `DELETE /api/message/:messageId`, `PATCH /api/message/:messageId/edit`, `POST /api/message/:messageId/reaction`, and `POST /api/message/batch/unread-counts` is rejected or filters unauthorized chats, and allowed members can perform the representative happy-path operations. Mock or neutralize Socket.IO emission only as needed to avoid requiring a live socket server; do not add socket authorization tests.</action>
  <verify>cd Backend/Chatify; npm test -- test/message/message.authorization.test.mjs</verify>
  <acceptance_criteria>
    - A non-member cannot fetch or create messages for another chat.
    - A non-member cannot read, edit, delete, react, or query unread counts for unauthorized chat data.
    - A member can fetch messages and create one message in a valid chat.
  </acceptance_criteria>
  <done>HTTP message authorization regressions fail the backend test suite.</done>
</task>

<task type="auto">
  <name>Task 6: Add CI gate and unignore workflow files</name>
  <files>.gitignore, .github/workflows/security-and-test-foundation.yml</files>
  <read_first>.gitignore, Backend/Chatify/package-lock.json, Frontend/Chatify/package-lock.json, Frontend/Chatify/package.json</read_first>
  <action>Fix the `.gitignore` rule that currently ignores `.github/` so workflows can be tracked, while preserving any intended local ignores. Add `.github/workflows/security-and-test-foundation.yml` for pull requests and pushes. Use `actions/checkout` and `actions/setup-node` with `node-version: 22`, `cache: npm`, and separate `cache-dependency-path` values for `Backend/Chatify/package-lock.json` and `Frontend/Chatify/package-lock.json`. Run backend `npm ci` plus `npm test`; run frontend `npm ci`, `npm run lint`, and `npm run build`. Do not require real secrets in CI.</action>
  <verify>git check-ignore -v .github/workflows/security-and-test-foundation.yml; cd Backend/Chatify; npm test; cd ../../Frontend/Chatify; npm run lint; npm run build</verify>
  <acceptance_criteria>
    - `git check-ignore` does not ignore `.github/workflows/security-and-test-foundation.yml`.
    - Workflow uses Node 22.
    - Workflow runs backend tests and frontend lint/build.
  </acceptance_criteria>
  <done>Repository automation can block backend security regressions and frontend breakage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd Backend/Chatify; npm test`
- [ ] `cd Frontend/Chatify; npm run lint`
- [ ] `cd Frontend/Chatify; npm run build`
- [ ] `git check-ignore -v .github/workflows/security-and-test-foundation.yml` produces no ignore match.
- [ ] `git status --short` shows no edits to `Frontend/Chatify/src/pages/chat/chat.tsx` from this plan.
</verification>

<success_criteria>

- Backend test harness exists and runs.
- Baseline auth lifecycle and HTTP message authorization coverage is committed.
- CI workflow exists, is trackable, and runs backend tests plus frontend lint/build.
- No real env secrets, OAuth credentials, Brevo calls, Render, Vercel, or local MongoDB are required.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-and-test-foundation/01-01-SUMMARY.md`.
</output>
