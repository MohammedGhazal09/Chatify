---
phase: 01-security-and-test-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: [.gitignore, Backend/Chatify/Utils/logger.mjs, Backend/Chatify/Utils/tokenCookieGenerator.mjs, Backend/Chatify/Middlewares/protectRoutes.mjs, Backend/Chatify/Middlewares/requestLogger.mjs, Backend/Chatify/Utils/requestQueue.mjs, Backend/Chatify/Controller/authController.mjs, Backend/Chatify/Controller/errController.mjs, Backend/Chatify/Controller/messageController.mjs, Backend/Chatify/Controller/chatController.mjs, Backend/Chatify/Config/DBConfig.mjs, Backend/Chatify/Config/passport.mjs, Backend/Chatify/Config/socket.mjs, Backend/Chatify/Services/emailService.mjs, Backend/Chatify/profile.json, Backend/Chatify/test/security/sensitive-logs.test.mjs, Frontend/Chatify/src/utils/logger.ts, Frontend/Chatify/src/api/axios.ts, Frontend/Chatify/src/hooks/useAuthQuery.ts, Frontend/Chatify/src/pages/login/login.tsx, Frontend/Chatify/src/pages/forgotPassword/forgotPassword.tsx]
autonomous: true
requirements: [SEC-02, TEST-04]
user_setup: []
must_haves:
  truths:
    - "Decision coverage: D-18 D-19 D-20 D-23 D-24 D-26."
    - Auth, token, OAuth, reset, socket, request, and frontend auth/reset logging do not emit raw tokens, token previews, reset codes, raw emails, OAuth profiles, or cookie option dumps by default.
    - `Backend/Chatify/profile.json` is removed from the repository if it contains generated OAuth/profile data and future profile dumps are ignored.
    - A blocking backend test or script fails on known forbidden sensitive log patterns.
  artifacts:
    - `Backend/Chatify/Utils/logger.mjs`
    - `Frontend/Chatify/src/utils/logger.ts`
    - `Backend/Chatify/test/security/sensitive-logs.test.mjs`
  key_links:
    - Sensitive backend modules import/use the backend logger or remove direct logging.
    - Sensitive frontend auth/reset modules import/use the frontend logger or remove direct logging.
    - Backend `npm test` runs the sensitive log gate.
---

<objective>
Replace sensitive debug logging with redacted operational logging and make sensitive-log regression checks blocking.

Purpose: The app currently logs token, cookie, email, OAuth, and reset-adjacent data in production-adjacent paths; Phase 1 must stop these leaks before broader chat work continues.
Output: Small backend/frontend redaction helpers, cleaned auth/reset/socket/request logs, generated profile artifact cleanup, and an automated sensitive-log gate.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-and-test-foundation/01-SPEC.md
@.planning/phases/01-security-and-test-foundation/01-CONTEXT.md
@.planning/phases/01-security-and-test-foundation/01-RESEARCH.md
@Backend/Chatify/Utils/tokenCookieGenerator.mjs
@Backend/Chatify/Middlewares/protectRoutes.mjs
@Backend/Chatify/Middlewares/requestLogger.mjs
@Backend/Chatify/Controller/authController.mjs
@Backend/Chatify/Controller/errController.mjs
@Backend/Chatify/Config/passport.mjs
@Backend/Chatify/Config/socket.mjs
@Backend/Chatify/Services/emailService.mjs
@Frontend/Chatify/src/api/axios.ts
@Frontend/Chatify/src/hooks/useAuthQuery.ts
@Frontend/Chatify/src/pages/login/login.tsx
@Frontend/Chatify/src/pages/forgotPassword/forgotPassword.tsx
</context>

<decision_traceability>
- D-18: Backend and frontend use small local redaction helpers.
- D-19: Sensitive logs are replaced rather than only hidden by environment checks.
- D-20: A lightweight log-pattern gate blocks known sensitive logging regressions.
- D-23: Phase keeps the approved three-plan split.
- D-24: `Backend/Chatify/profile.json` is inspected and removed or ignored if generated profile data.
- D-26: Plan verification forbids edits to `Frontend/Chatify/src/pages/chat/chat.tsx`.
</decision_traceability>

<tasks>

<task type="auto">
  <name>Task 1: Add backend and frontend redaction helpers</name>
  <files>Backend/Chatify/Utils/logger.mjs, Frontend/Chatify/src/utils/logger.ts</files>
  <read_first>.planning/codebase/CONVENTIONS.md, .planning/phases/01-security-and-test-foundation/01-CONTEXT.md</read_first>
  <action>Create small local logging helpers. Backend helper should expose methods such as `info`, `warn`, `error`, and `redactValue` or `redactFields`, replacing token-like strings, email addresses, reset codes, cookie objects, and OAuth profile payloads with stable redacted labels. Frontend helper should do the same for browser console usage. Gate verbose/debug output by environment, but do not depend on `NODE_ENV` alone to protect secrets.</action>
  <verify>cd Backend/Chatify; npm test -- test/security/sensitive-logs.test.mjs</verify>
  <acceptance_criteria>
    - Helpers never return raw emails, JWT-like strings, reset codes, or cookie object contents in log payloads.
    - Helpers support operational event names without full user identifiers.
    - No new logging dependency is added.
  </acceptance_criteria>
  <done>Shared redaction primitives exist for Phase 1 logging cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: Replace backend sensitive direct logs</name>
  <files>Backend/Chatify/Utils/tokenCookieGenerator.mjs, Backend/Chatify/Middlewares/protectRoutes.mjs, Backend/Chatify/Middlewares/requestLogger.mjs, Backend/Chatify/Utils/requestQueue.mjs, Backend/Chatify/Controller/authController.mjs, Backend/Chatify/Controller/errController.mjs, Backend/Chatify/Controller/messageController.mjs, Backend/Chatify/Controller/chatController.mjs, Backend/Chatify/Config/DBConfig.mjs, Backend/Chatify/Config/passport.mjs, Backend/Chatify/Config/socket.mjs, Backend/Chatify/Services/emailService.mjs</files>
  <read_first>Backend/Chatify/Utils/tokenCookieGenerator.mjs, Backend/Chatify/Middlewares/protectRoutes.mjs, Backend/Chatify/Controller/authController.mjs, Backend/Chatify/Controller/errController.mjs, Backend/Chatify/Config/passport.mjs, Backend/Chatify/Services/emailService.mjs</read_first>
  <action>Remove or replace direct `console.log` / `console.error` calls that expose user ids, emails, token lengths/previews, token verification details, cookie options, raw error payloads, reset email details, OAuth profiles, or request bodies. Keep useful operational events such as `auth.token_issued`, `auth.token_invalid`, `reset.email_send_failed`, and `socket.emit_failed` with redacted or absent identifiers. In `errController.mjs`, expand development body sanitization for password/reset fields and avoid returning raw email/user metadata in error diagnostics.</action>
  <verify>rg -n "Token preview|User Email|Full cookie options|Available cookies|OAuth Error:|console\\.log\\(err\\)|request body|resetCode|profile" Backend/Chatify</verify>
  <acceptance_criteria>
    - Token generator does not log user id, email, token length, token preview, or cookie options.
    - Protect middleware does not log token previews, available cookies, or decoded user id.
    - OAuth and reset paths do not log raw profile, email, reset code, or provider payloads.
    - Development error responses sanitize password, code, token, newPassword, email, and cookie-like fields.
  </acceptance_criteria>
  <done>Backend auth/security logs are operational without leaking secrets or identifiers.</done>
</task>

<task type="auto">
  <name>Task 3: Replace frontend auth/reset debug logs</name>
  <files>Frontend/Chatify/src/utils/logger.ts, Frontend/Chatify/src/api/axios.ts, Frontend/Chatify/src/hooks/useAuthQuery.ts, Frontend/Chatify/src/pages/login/login.tsx, Frontend/Chatify/src/pages/forgotPassword/forgotPassword.tsx</files>
  <read_first>Frontend/Chatify/src/api/axios.ts, Frontend/Chatify/src/hooks/useAuthQuery.ts, Frontend/Chatify/src/pages/login/login.tsx, Frontend/Chatify/src/pages/forgotPassword/forgotPassword.tsx</read_first>
  <action>Remove raw `console.log(data)`, raw Axios error logs, OAuth error detail dumps, and reset error logs from frontend auth/reset paths. Replace only the useful operational cases with the frontend logger and redacted event names. Keep user-facing error messages unchanged unless they reveal account existence or secrets. Do not modify `Frontend/Chatify/src/pages/chat/chat.tsx`.</action>
  <verify>cd Frontend/Chatify; npm run lint; npm run build; rg -n "console\\.(log|error|warn)" src/pages/login src/pages/forgotPassword src/api src/hooks</verify>
  <acceptance_criteria>
    - Login page no longer logs submitted form data or raw OAuth error details.
    - Forgot-password page no longer logs raw Axios errors or reset inputs.
    - Axios rate-limit/session logs are redacted and do not include request bodies or credentials.
  </acceptance_criteria>
  <done>Frontend auth/reset console paths no longer leak sensitive data.</done>
</task>

<task type="auto">
  <name>Task 4: Remove generated OAuth profile artifact and ignore future dumps</name>
  <files>Backend/Chatify/profile.json, .gitignore</files>
  <read_first>Backend/Chatify/profile.json, .gitignore</read_first>
  <action>Inspect `Backend/Chatify/profile.json` without copying its contents into summaries. If it contains generated OAuth/profile data or personal email/profile fields, delete it from the repository and add a targeted ignore rule such as `Backend/Chatify/profile.json` or `**/profile.json` if project-safe. Do not delete unrelated profile files outside this path.</action>
  <verify>git status --short Backend/Chatify/profile.json .gitignore; git check-ignore -v Backend/Chatify/profile.json</verify>
  <acceptance_criteria>
    - `Backend/Chatify/profile.json` is deleted if it contains OAuth/profile data.
    - Future generated profile dumps at that path are ignored.
    - No profile contents are quoted in logs, summaries, or docs.
  </acceptance_criteria>
  <done>Committed generated profile data is removed and cannot be re-added accidentally.</done>
</task>

<task type="auto">
  <name>Task 5: Add sensitive-log regression gate</name>
  <files>Backend/Chatify/test/security/sensitive-logs.test.mjs, Backend/Chatify/package.json</files>
  <read_first>Backend/Chatify/test/security/csrf.test.mjs, Backend/Chatify/package.json, .planning/phases/01-security-and-test-foundation/01-SPEC.md</read_first>
  <action>Add a Vitest test that scans the sensitive backend and frontend files listed in this plan for forbidden logging patterns. Fail on token preview/length logs, raw email logs in auth/reset/token paths, reset code logs, full cookie option logs, `console.log(data)` in login, `console.log(err)` in reset, raw OAuth profile dumps, and committed `Backend/Chatify/profile.json`. Keep the scanner narrow enough that harmless model field definitions do not fail. Ensure `npm test` runs this gate by default.</action>
  <verify>cd Backend/Chatify; npm test -- test/security/sensitive-logs.test.mjs; npm test</verify>
  <acceptance_criteria>
    - The test fails if a forbidden token/email/reset/cookie log pattern is reintroduced.
    - The test passes after cleanup.
    - The test is part of backend `npm test`.
  </acceptance_criteria>
  <done>Sensitive logging regressions block Phase 1 verification.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd Backend/Chatify; npm test`
- [ ] `cd Frontend/Chatify; npm run lint`
- [ ] `cd Frontend/Chatify; npm run build`
- [ ] `rg -n "Token preview|User Email|Full cookie options|Available cookies|console\\.log\\(data\\)|console\\.log\\(err\\)|resetCode" Backend/Chatify Frontend/Chatify/src` returns no sensitive auth/reset/token logging matches.
- [ ] `git status --short` shows no edits to `Frontend/Chatify/src/pages/chat/chat.tsx` from this plan.
</verification>

<success_criteria>

- Sensitive logs are removed or routed through redaction helpers.
- Generated profile data is removed and ignored.
- Backend tests include a log-pattern gate that fails on known leak regressions.
- Phase 1 security verification remains green after logging cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-and-test-foundation/01-03-SUMMARY.md`.
</output>
