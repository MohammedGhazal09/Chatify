---
phase: 07-messenger-functional-parity-restoration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Frontend/Chatify/src/pages/chat/chat.tsx, Frontend/Chatify/src/pages/chat/components/Phase06VisualFixture.ts, Frontend/Chatify/src/pages/chat/components/Phase06VisualFixture.test.tsx, Frontend/Chatify/src/pages/chat/fixtureLeakGuard.test.ts, Frontend/Chatify/e2e/fixtures/phase06VisualFixture.ts, Frontend/Chatify/e2e/chat-ui-smoke.spec.ts]
autonomous: true
requirements: [PARITY-01, PARITY-03, UI-01, UI-05, RT-04, TEST-03, TEST-05]
user_setup: []
must_haves:
  truths:
    - "Decision coverage: D-01 D-02 D-03 D-04 D-05 D-06 D-07 D-08 D-21 D-24 D-25."
    - "Execution must be inline in the current Codex thread; do not use subagents."
    - "Product chat runtime must not branch on `chatVisualSmoke`, `Phase06VisualFixture`, `PHASE06_`, or Phase 06 fixture exports."
    - "Visual smoke data may remain only in explicit test/e2e fixture paths."
    - "Visual-only Playwright smoke may keep deterministic HTTP mocks and may abort sockets, but this plan must not make the product runtime depend on that mode."
  artifacts:
    - "`Frontend/Chatify/e2e/fixtures/phase06VisualFixture.ts` or equivalent test-only visual fixture path."
    - "A static fixture-leak guard in Vitest or an equivalent checked verification script."
    - "`chat.tsx` socket enablement based on real selected-chat/auth/offline state only."
  key_links:
    - "`chat.tsx` remains the orchestration layer and keeps TanStack Query/Zustand/Socket.IO ownership boundaries."
    - "`chat-ui-smoke.spec.ts` imports visual data from e2e/test support, not production components."
---

<objective>
Remove the Phase 06 visual-smoke shortcut from production chat runtime and install guardrails that prevent test fixture data from leaking back into product UI paths.

Purpose: Phase 07 cannot prove functional parity while `chat.tsx` can disable sockets with `chatVisualSmoke=phase06` and Playwright can import production fixture data.
Output: test-only visual fixtures, production runtime without Phase 06 branches, and a repeatable static guard.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-messenger-functional-parity-restoration/07-SPEC.md
@.planning/phases/07-messenger-functional-parity-restoration/07-CONTEXT.md
@.planning/phases/07-messenger-functional-parity-restoration/07-RESEARCH.md
@.planning/phases/06-messenger-visual-parity/06-CONTEXT.md
@Frontend/Chatify/src/pages/chat/chat.tsx
@Frontend/Chatify/src/pages/chat/components/Phase06VisualFixture.ts
@Frontend/Chatify/e2e/chat-ui-smoke.spec.ts
@Frontend/Chatify/src/test/chatFixtures.ts
@Frontend/Chatify/src/hooks/useChatSocket.ts
@Frontend/Chatify/package.json
@C:/Users/saieh/.agents/skills/react-best-practices/SKILL.md
@C:/Users/saieh/.agents/skills/tanstack-query/SKILL.md
@C:/Users/saieh/.agents/skills/websocket-engineer/SKILL.md
@C:/Users/saieh/.agents/skills/e2e-testing-patterns/SKILL.md
</context>

<decision_traceability>
- D-01: Remove `chatVisualSmoke=phase06` from product runtime entirely.
- D-02: Move Playwright-only visual fixture data to `Frontend/Chatify/e2e/fixtures/`.
- D-03: Keep reusable component/hook fixture builders in `Frontend/Chatify/src/test/` only when needed by Vitest.
- D-04: Preserve Phase 06 screenshot fixtures as historical visual evidence, but current behavior tests must use non-Phase-6 data.
- D-05: Add a static guard that fails on production chat runtime references to Phase 06 fixture identifiers.
- D-06: Repair targeted seams only; do not rewrite the chat page from scratch.
- D-07: Keep `chat.tsx` as orchestration layer.
- D-08: Preserve TanStack Query, Zustand, and Socket.IO ownership boundaries.
- D-21: Functional tests later must not rely on global socket abort; visual-only tests may.
- D-24: Screenshots remain evidence only after behavior interactions in later plans.
- D-25: Store Phase 07 evidence under this phase directory in later plans.
</decision_traceability>

<tasks>

<task type="tdd">
  <name>Task 1: Move Phase 06 visual fixture data out of production chat components</name>
  <files>Frontend/Chatify/src/pages/chat/components/Phase06VisualFixture.ts, Frontend/Chatify/src/pages/chat/components/Phase06VisualFixture.test.tsx, Frontend/Chatify/e2e/fixtures/phase06VisualFixture.ts, Frontend/Chatify/e2e/chat-ui-smoke.spec.ts</files>
  <read_first>Frontend/Chatify/src/pages/chat/components/Phase06VisualFixture.ts, Frontend/Chatify/src/pages/chat/components/Phase06VisualFixture.test.tsx, Frontend/Chatify/e2e/chat-ui-smoke.spec.ts, Frontend/Chatify/src/test/chatFixtures.ts</read_first>
  <action>Move the Phase 06 visual fixture module from the production component directory into an explicit e2e/test fixture path. Update Playwright imports to read from that test-only path. Remove or relocate the colocated `Phase06VisualFixture.test.tsx` so production component tests no longer validate demo data as a product component. Preserve visual fixture semantics only for deterministic Phase 06 screenshot smoke and do not import the fixture from any `src/pages/chat/components` runtime file.</action>
  <verify>cd Frontend/Chatify; npm test -- --run src/pages/chat; npm run lint</verify>
  <acceptance_criteria>
    - `Frontend/Chatify/src/pages/chat/components/Phase06VisualFixture.ts` no longer exists as a production component module.
    - `Frontend/Chatify/e2e/chat-ui-smoke.spec.ts` imports visual data from `Frontend/Chatify/e2e/fixtures/` or another explicit test-only location.
    - No production chat component imports Phase 06 visual fixture exports.
    - Existing Phase 06 visual-smoke data remains available only to tests that intentionally need historical screenshot parity.
  </acceptance_criteria>
  <done>Phase 06 visual data is test-only and no longer looks like a production chat component.</done>
</task>

<task type="tdd">
  <name>Task 2: Remove the `chatVisualSmoke=phase06` runtime branch from chat orchestration</name>
  <files>Frontend/Chatify/src/pages/chat/chat.tsx, Frontend/Chatify/src/hooks/useChatSocket.ts, Frontend/Chatify/src/pages/chat/hooks/useChatTheme.ts, Frontend/Chatify/e2e/chat-ui-smoke.spec.ts</files>
  <read_first>Frontend/Chatify/src/pages/chat/chat.tsx, Frontend/Chatify/src/hooks/useChatSocket.ts, Frontend/Chatify/src/pages/chat/hooks/useChatTheme.ts, Frontend/Chatify/e2e/chat-ui-smoke.spec.ts</read_first>
  <action>Delete `isPhase06VisualSmoke()`, `isVisualSmokeMode`, and the `chatVisualSmoke` query check from `chat.tsx`. Make `useChatSocket` enablement depend on real conditions only: selected chat id, authentication, browser online/session state, and existing hook constraints. Keep `chatTheme` URL override because it is a theme verification helper, not a product fixture shortcut. Update visual smoke opening URLs to omit `chatVisualSmoke=phase06` and rely on route-mocked HTTP data instead.</action>
  <verify>cd Frontend/Chatify; npm test -- --run src/pages/chat/hooks/useChatTheme.test.tsx src/hooks/useChatSocket.test.tsx; npm run build</verify>
  <acceptance_criteria>
    - `chat.tsx` contains no `chatVisualSmoke`, `isPhase06VisualSmoke`, `isVisualSmokeMode`, `Phase06VisualFixture`, `PHASE06_`, or `phase06` runtime branch.
    - Socket behavior is no longer disabled by a visual-smoke query flag.
    - Theme URL override remains available for deterministic light/dark verification.
    - Build succeeds after the runtime branch is removed.
  </acceptance_criteria>
  <done>Production chat runtime cannot opt into Phase 06 static visual mode.</done>
</task>

<task type="tdd">
  <name>Task 3: Add a static fixture-leak guard for production chat runtime</name>
  <files>Frontend/Chatify/src/pages/chat/fixtureLeakGuard.test.ts, Frontend/Chatify/package.json</files>
  <read_first>Frontend/Chatify/package.json, Frontend/Chatify/vite.config.ts, Frontend/Chatify/src/test/setup.ts, Frontend/Chatify/src/pages/chat/chat.tsx</read_first>
  <action>Add a Vitest guard that scans production chat runtime files under `Frontend/Chatify/src/pages/chat` and fails if non-test files contain `Phase06VisualFixture`, `PHASE06_`, `phase06`, or `chatVisualSmoke`. Exclude `.test.*`, `src/test`, and `e2e/fixtures` paths. Keep the guard simple and deterministic; it should protect runtime source, not ban historical e2e visual fixture files.</action>
  <verify>cd Frontend/Chatify; npm test -- --run src/pages/chat/fixtureLeakGuard.test.ts; rg -n "Phase06VisualFixture|PHASE06_|phase06|chatVisualSmoke" src/pages/chat --glob "!**/*.test.*"</verify>
  <acceptance_criteria>
    - The guard fails if a non-test chat runtime file reintroduces Phase 06 fixture identifiers.
    - The guard passes with e2e/test-only Phase 06 visual fixtures still present outside production runtime.
    - The manual `rg` verification shows no runtime leakage in `src/pages/chat` excluding tests.
  </acceptance_criteria>
  <done>Future production fixture leakage becomes a test failure.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd Frontend/Chatify; npm test -- --run src/pages/chat/fixtureLeakGuard.test.ts src/hooks/useChatSocket.test.tsx src/pages/chat/hooks/useChatTheme.test.tsx`
- [ ] `cd Frontend/Chatify; npm run lint`
- [ ] `cd Frontend/Chatify; npm run build`
- [ ] `rg -n "Phase06VisualFixture|PHASE06_|phase06|chatVisualSmoke" Frontend/Chatify/src/pages/chat --glob "!**/*.test.*"` reviewed with no production runtime matches.
- [ ] `Frontend/Chatify/e2e/chat-ui-smoke.spec.ts` imports visual data from test/e2e fixtures only.
</verification>

<success_criteria>
- Product runtime has no Phase 06 visual-smoke query flag.
- Phase 06 visual fixtures are test-only.
- Socket enablement is no longer gated by a visual screenshot mode.
- Static guardrails make fixture leakage fail repeatably.
- `npm test`, lint, and build remain green for the targeted frontend scope.
</success_criteria>

<output>
After completion, create `.planning/phases/07-messenger-functional-parity-restoration/07-01-SUMMARY.md`.
</output>
