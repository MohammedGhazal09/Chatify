---
phase: 13-realtime-call-and-video-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Backend/Chatify/Models/callSessionModel.mjs, Backend/Chatify/Utils/callSessionState.mjs, Backend/Chatify/Utils/callIceConfig.mjs, Backend/Chatify/Utils/callSocketContract.mjs, Backend/Chatify/Config/socket.mjs, Backend/Chatify/Models/messageModel.mjs, Backend/Chatify/Utils/messageState.mjs, Backend/Chatify/test/socket/socket.calls.test.mjs, Backend/Chatify/test/socket/socket.call-auth.test.mjs, Backend/Chatify/test/socket/socket.call-blocking.test.mjs]
autonomous: true
requirements: [CALL-01, CALL-02, CALL-03, CALL-04, BLOCK-02, RT-01, RT-02, TEST-02]
user_setup:
  - "For production-ready call evidence, configure backend ICE/TURN environment values before deployment verification. Do not commit TURN credentials. Local execution may proceed with documented STUN-only development config."
must_haves:
  truths:
    - "Decision coverage: D-01 D-02 D-03 D-04 D-05 D-06 D-07 D-08 D-09 D-10 D-11 D-12 D-13 D-15 D-16 D-32 D-33 D-35 D-36 D-40."
    - "Execution must be inline in the current Codex thread; do not use subagents."
    - "The server owns call sessions, transition validity, busy state, timeout, blocked-state rejection, stale call id rejection, and cross-chat rejection."
    - "Socket identity comes from the authenticated cookie handshake. Client-provided user ids are never trusted for call authorization."
    - "No SDP bodies, ICE candidates, media streams, device labels, tokens, cookies, or private WebRTC payloads may be persisted or logged."
    - "Offline or no reachable callee socket must not be represented as a successful live ring."
  artifacts:
    - "CallSession metadata model or equivalent durable lifecycle contract."
    - "Call state transition helper and structured socket acknowledgement payloads."
    - "Backend-controlled ICE config delivery with production TURN readiness flag."
    - "Socket.IO call event family with auth, membership, block, busy, timeout, stale, cross-chat, multi-socket, and rate-limit tests."
  key_links:
    - "`13-SPEC.md` REQ-13-004 through REQ-13-009 lock server authority, signaling, permissions, lifecycle, and offline behavior."
    - "`13-CONTEXT.md` D-05 through D-17 lock backend call session and signaling decisions."
    - "`13-RESEARCH.md` recommends server authority before frontend WebRTC wiring."
---

<objective>
Build the backend call session and signaling authority.

Purpose: every Chatify call attempt has a server-owned lifecycle, truthful acknowledgement, authorized transient signaling path, and blocked/offline/busy protection before any UI is enabled.
Output: `CallSession` metadata persistence, call state transition helpers, ICE config delivery, `call:*` Socket.IO handlers, call rate limits, and backend socket tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-realtime-call-and-video-implementation/13-SPEC.md
@.planning/phases/13-realtime-call-and-video-implementation/13-CONTEXT.md
@.planning/phases/13-realtime-call-and-video-implementation/13-RESEARCH.md
@.planning/phases/10.1-production-message-delivery-reliability-repair/10.1-CONTEXT.md
@.planning/phases/11-conversation-controls-and-user-safety-implementation/11-CONTEXT.md
@.planning/phases/12-live-media-voice-and-identity-implementation/12-CONTEXT.md
@Backend/Chatify/Config/socket.mjs
@Backend/Chatify/Utils/chatAccess.mjs
@Backend/Chatify/Utils/conversationControls.mjs
@Backend/Chatify/Models/chatModel.mjs
@Backend/Chatify/Models/messageModel.mjs
@Backend/Chatify/test/helpers/socketClient.mjs
@Backend/Chatify/test/helpers/socketServer.mjs
@Backend/Chatify/test/socket/socket.authorization.test.mjs
@Backend/Chatify/test/socket/socket.blocking.test.mjs
@Backend/Chatify/test/socket/socket.message-state.test.mjs
@C:/Users/saieh/.agents/skills/websocket-engineer/SKILL.md
@C:/Users/saieh/.agents/skills/api-and-interface-design/SKILL.md
</context>

<decision_traceability>
- D-01: Completion remains gated on final evidence from Phases 10.1, 11, and 12.
- D-02: Only direct-message one-to-one audio/video calling is in scope.
- D-03: Preserve the existing MERN, Socket.IO, TanStack Query, Zustand, Tailwind, and npm layout.
- D-04: Do not use subagents.
- D-05: Add lightweight `CallSession` metadata.
- D-06: Persist only safe call activity metadata for missed, rejected, failed, and ended calls.
- D-07: Keep SDP, ICE, media, device labels, and raw WebRTC payloads transient.
- D-08: Use a dedicated `call:*` Socket.IO event family.
- D-09: Every client call action requires a structured server acknowledgement.
- D-10: Server owns call creation, transition validity, timeout, busy, blocked, stale, and cross-chat rejection.
- D-11: First server-accepted simultaneous call wins.
- D-12: Ring all callee sockets and synchronize first decision to other sockets.
- D-13: Add call-specific socket limits and call-start cooldown.
- D-15: Deliver ICE config from backend-controlled environment values.
- D-16: Do not hardcode TURN credentials; block production readiness if TURN is missing.
- D-32: Offline or no reachable authenticated socket must not create a false ring or recipient missed record.
- D-33: Blocking during active call ends the call and rejects further signaling.
- D-35: Preserve one active or ringing call per user and 30 second ring timeout.
- D-36: Backend socket tests must cover the full call lifecycle and failure matrix.
- D-40: Message reliability regression checks remain mandatory after call socket changes.
</decision_traceability>

<tasks>

<task type="tdd">
  <name>Task 1: Add CallSession metadata and transition helper</name>
  <files>Backend/Chatify/Models/callSessionModel.mjs, Backend/Chatify/Utils/callSessionState.mjs, Backend/Chatify/test/socket/socket.calls.test.mjs</files>
  <read_first>Backend/Chatify/Models/chatModel.mjs, Backend/Chatify/Models/messageModel.mjs, Backend/Chatify/Utils/chatAccess.mjs, Backend/Chatify/Utils/conversationControls.mjs</read_first>
  <action>Create a lightweight call session model and state helper. Fields must include `callId`, `chatId`, `callerId`, `calleeId`, `mode`, `status`, `startedAt`, `ringingAt`, `answeredAt`, `endedAt`, and `endedReason`. Add helper functions for start, accept, reject, end, timeout, fail, sync, and active/ringing lookup. Enforce direct-chat scope, membership, one active or ringing call per user, 30 second timeout, and first-accepted session wins.</action>
  <verify>cd Backend/Chatify; npm test -- --run test/socket/socket.calls.test.mjs</verify>
  <acceptance_criteria>
    - Audio and video sessions can be created only for direct chats with exactly two members.
    - Group chat, malformed ids, non-member user, blocked conversation, and duplicate active/ringing sessions are rejected.
    - Invalid state transitions, stale call ids, and cross-chat call ids return stable structured error codes.
    - Timeout changes ringing sessions to missed after 30 seconds.
    - Model/helper code never stores SDP, ICE, media streams, device labels, tokens, cookies, or private payloads.
  </acceptance_criteria>
  <done>Backend has one authoritative call lifecycle contract.</done>
</task>

<task type="tdd">
  <name>Task 2: Add backend ICE config and call socket contract</name>
  <files>Backend/Chatify/Utils/callIceConfig.mjs, Backend/Chatify/Utils/callSocketContract.mjs, Backend/Chatify/Config/socket.mjs, Backend/Chatify/test/socket/socket.call-auth.test.mjs</files>
  <read_first>Backend/Chatify/Config/socket.mjs, Backend/Chatify/test/socket/socket.authorization.test.mjs, Backend/Chatify/test/helpers/socketClient.mjs</read_first>
  <action>Add backend-controlled ICE config parsing and expose a safe `callConfig` payload through `socket:ready` and `call:start` acknowledgement. Add structured payload builders for `call:start`, `call:incoming`, `call:accept`, `call:reject`, `call:end`, `call:offer`, `call:answer`, `call:ice-candidate`, and `call:sync`. Every client-originated action must return `{ ok, event, code?, callId?, status?, message? }` and must support acknowledgement timeouts on the frontend.</action>
  <verify>cd Backend/Chatify; npm test -- --run test/socket/socket.call-auth.test.mjs</verify>
  <acceptance_criteria>
    - `socket:ready` includes no secrets beyond ICE values intentionally required by WebRTC clients.
    - TURN credentials are read from environment or omitted; no hardcoded TURN credentials appear in source.
    - Production readiness flag is false or warning-bearing when TURN is absent.
    - All call client actions return a structured acknowledgement.
    - Socket errors are private-resource safe and do not leak peer emails, tokens, cookie metadata, SDP, or ICE values.
  </acceptance_criteria>
  <done>Call transport contract is explicit and backend-owned.</done>
</task>

<task type="tdd">
  <name>Task 3: Implement call Socket.IO handlers and targeted sync</name>
  <files>Backend/Chatify/Config/socket.mjs, Backend/Chatify/Utils/callSessionState.mjs, Backend/Chatify/Utils/callSocketContract.mjs, Backend/Chatify/test/socket/socket.calls.test.mjs, Backend/Chatify/test/socket/socket.call-blocking.test.mjs</files>
  <read_first>Backend/Chatify/Config/socket.mjs, Backend/Chatify/Utils/conversationControls.mjs, Backend/Chatify/test/socket/socket.blocking.test.mjs, Backend/Chatify/test/socket/socket.presence-reconnect.test.mjs</read_first>
  <action>Register `call:*` handlers in the authenticated socket connection. Use `assertChatMember`, direct-chat peer resolution, block checks, call rate limits, and user-socket targeting. `call:start` must ring all reachable callee sockets and fail truthfully when none are reachable. `call:accept`, `call:reject`, and `call:end` must synchronize all participant sockets. `call:offer`, `call:answer`, and `call:ice-candidate` must be transient, authorized, participant-only forwards and must not be logged or persisted.</action>
  <verify>cd Backend/Chatify; npm test -- --run test/socket/socket.calls.test.mjs test/socket/socket.call-blocking.test.mjs</verify>
  <acceptance_criteria>
    - Incoming calls are emitted only to authenticated callee sockets.
    - Multi-tab callee sessions all ring, and first accept/reject/end wins.
    - Offline callee returns unavailable or failed and does not create a false recipient missed record.
    - Busy users receive a busy acknowledgement or sync.
    - Blocked users cannot start or continue signaling, and an active call ends if a block becomes active.
    - SDP and ICE payloads are forwarded only to the peer sockets and never persisted.
  </acceptance_criteria>
  <done>Backend can safely signal direct-message calls.</done>
</task>

<task type="verification">
  <name>Task 4: Lock backend call regression coverage</name>
  <files>Backend/Chatify/test/socket/socket.calls.test.mjs, Backend/Chatify/test/socket/socket.call-auth.test.mjs, Backend/Chatify/test/socket/socket.call-blocking.test.mjs, Backend/Chatify/test/socket/socket.message-state.test.mjs, Backend/Chatify/test/socket/socket.authorization.test.mjs</files>
  <read_first>Backend/Chatify/package.json, Backend/Chatify/test/socket/socket.message-state.test.mjs, Backend/Chatify/test/socket/socket.authorization.test.mjs</read_first>
  <action>Run the targeted backend call and message-state regression suite. Fix any regression caused by new call listeners, rate-limit windows, socket cleanup, or helper imports before proceeding to frontend work.</action>
  <verify>cd Backend/Chatify; npm test -- --run test/socket/socket.calls.test.mjs test/socket/socket.call-auth.test.mjs test/socket/socket.call-blocking.test.mjs test/socket/socket.message-state.test.mjs test/socket/socket.authorization.test.mjs</verify>
  <acceptance_criteria>
    - Call lifecycle tests pass.
    - Socket auth and authorization tests pass.
    - Message state tests still prove socket calls did not break one-send, realtime receive, or receipt truth.
    - Socket cleanup clears call timers/windows where applicable.
  </acceptance_criteria>
  <done>Backend call foundation is ready for frontend wiring.</done>
</task>

</tasks>

<verification>
- `cd Backend/Chatify; npm test -- --run test/socket/socket.calls.test.mjs`
- `cd Backend/Chatify; npm test -- --run test/socket/socket.call-auth.test.mjs test/socket/socket.call-blocking.test.mjs`
- `cd Backend/Chatify; npm test -- --run test/socket/socket.message-state.test.mjs test/socket/socket.authorization.test.mjs`
- `cd Backend/Chatify; npm test`
</verification>

<success_criteria>
- Server owns all call sessions and transitions.
- Call events are authenticated, membership-scoped, block-safe, and rate-limited.
- Offline, busy, timeout, stale, duplicate, and cross-chat paths return truthful acknowledgements.
- Signaling payloads are transient and participant-only.
- ICE config is backend controlled and production TURN readiness is explicit.
- Backend message/socket reliability tests still pass after call handlers are added.
</success_criteria>
