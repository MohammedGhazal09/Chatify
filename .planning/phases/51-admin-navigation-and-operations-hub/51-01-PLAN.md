# Phase 51 Plan 01: Hub Route, Access State, And Sidebar Shortcut

## Tasks

- Add `AdminHub.tsx`.
- Register `/admin` in `App.tsx`.
- Update the chat sidebar admin shortcut to `/admin`.
- Add English/Arabic i18n keys for the hub.
- Add focused tests for admin/non-admin hub access and sidebar shortcut behavior.

## Acceptance

- Non-admin users see restricted state and no admin API calls.
- Admin users see the operations hub route.
- Sidebar shortcut points to `/admin`.

## Recommendation

Keep the first plan about route and access only, so permission behavior is correct before summary data is layered in.
