# 50-02 Plan: Local Smoke And Visual QA Refresh

## Goal

Refresh local/browser release evidence for production smoke config, operations readiness, and release-critical UI surfaces.

## Requirements

- PROD-04
- TEST-04
- TEST-05
- V2-I18N-02
- V2-DELIVERY-HEALTH-05

## Tasks

1. Run production smoke config gate:
   - `npm --prefix Frontend/Chatify run test:e2e:prod -- e2e/production-smoke-config.spec.ts --workers=1`
2. Run operations guard:
   - `npm run ops:check`
3. Run Hercules-compatible visual QA with Playwright fallback:
   - set `HERCULES_ARTIFACT_DIR` to `.planning/phases/50-release-candidate-evidence-and-production-smoke-refresh/visual-qa`
   - set `CHATIFY_CHAT_SMOKE_ARTIFACT_DIR` to `.planning/phases/50-release-candidate-evidence-and-production-smoke-refresh/visual-qa/screenshots`
   - run `npx playwright test e2e/chat-ui-smoke.spec.ts e2e/admin-delivery-health.spec.ts --config=playwright.config.ts`
   - capture screenshots under the Phase 50 visual QA directory
4. Write `50-VISUAL-QA.md`:
   - include identity evidence, coverage ledger, visual evidence, logic evidence, console/network evidence, runner note, and remaining blockers

## Artifacts This Plan Produces

- `visual-qa/screenshots/*`
- `50-VISUAL-QA.md`
- `50-02-SUMMARY.md`

## Verification

- Production smoke config test passes.
- Operations guard passes.
- Playwright visual QA pass records screenshots and workflow assertions.

## Risks

- Mocked local browser checks do not prove hosted production behavior.
- Existing chat smoke screenshot path override must not change default Phase 8 behavior.
- Hercules local runner may be unusable without subagents/provider keys; label fallback honestly.
