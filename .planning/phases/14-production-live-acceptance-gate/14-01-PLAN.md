---
phase: 14-production-live-acceptance-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Frontend/Chatify/playwright.production.config.ts, Frontend/Chatify/package.json, Frontend/Chatify/e2e/pages/productionSmoke.ts, Frontend/Chatify/e2e/production-smoke-config.spec.ts, Frontend/Chatify/e2e/pages/phase14ProductionAcceptance.ts, .planning/phases/14-production-live-acceptance-gate/14-LIVE-ACCEPTANCE.md]
autonomous: true
requirements: [PROD-01, PROD-02, PROD-04, TEST-01, TEST-05]
user_setup:
  - "No production credentials are required to complete the no-env and config-validation parts of this plan."
  - "Do not print, commit, or write raw production credentials, cookies, tokens, full emails, request bodies, or private payloads."
must_haves:
  truths:
    - "Execution must be inline in the current Codex thread; do not use subagents."
    - "Production acceptance must never silently fall back to local Vite, route mocks, fixture files, or default local URLs."
    - "Missing env vars, invalid URLs, and disabled opt-in are blocked/skipped production-gate states, not passing readiness states."
    - "Phase 10 and Phase 10.1 production smoke helpers and artifact paths must keep working."
  artifacts:
    - "Dedicated production Playwright config with no local web server."
    - "`test:e2e:prod` npm script."
    - "Phase 14 production helper/reporting module or carefully extended productionSmoke helper."
    - "Sanitized `14-LIVE-ACCEPTANCE.md` blocked/pass report skeleton."
  key_links:
    - "`14-SPEC.md` requirements 1, 2, 3, 10, 11, and 12."
    - "`14-CONTEXT.md` D-01 through D-13 and D-28 through D-34."
    - "`14-RESEARCH.md` Playwright auth/projects/recording and Vercel/Render environment findings."
---

<objective>
Build the production acceptance harness, environment contract, and evidence reporter.

Purpose: make Phase 14 impossible to pass accidentally with local fixtures, local servers, missing credentials, or unsanitized artifacts.
Output: production Playwright config, focused npm script, env/config validation, Phase 14 reporting helpers, blocker aggregation, and a sanitized acceptance artifact skeleton.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-production-live-acceptance-gate/14-SPEC.md
@.planning/phases/14-production-live-acceptance-gate/14-CONTEXT.md
@.planning/phases/14-production-live-acceptance-gate/14-RESEARCH.md
@.planning/phases/14-production-live-acceptance-gate/14-VALIDATION.md
@Frontend/Chatify/package.json
@Frontend/Chatify/playwright.config.ts
@Frontend/Chatify/e2e/pages/productionSmoke.ts
@Frontend/Chatify/e2e/production-smoke-config.spec.ts
@Frontend/Chatify/e2e/chat-production-reality.spec.ts
@Frontend/Chatify/e2e/chat-production-delivery-reliability.spec.ts
@C:/Users/saieh/.agents/skills/e2e-testing-patterns/SKILL.md
@C:/Users/saieh/.agents/skills/api-testing/SKILL.md
@C:/Users/saieh/.agents/skills/deployment-procedures/SKILL.md
</context>

<decision_traceability>
- D-01: Add a dedicated Phase 14 production acceptance spec rather than extending older smoke specs.
- D-02: Add shared Phase 14 helper code adjacent to or inside `productionSmoke.ts` while preserving old artifact paths.
- D-03: Write Phase 14 evidence to `14-LIVE-ACCEPTANCE.md`.
- D-04: Use structured markdown evidence with commands, origins, redacted accounts, pass/fail rows, blockers, and risks.
- D-05: Add a production Playwright config with no local web server.
- D-06: Keep local Playwright config intact.
- D-07: Prefer one serial suite with `test.step` sections and final blocker aggregation.
- D-08: Hard setup failures may stop early after writing a blocked artifact.
- D-09: Reuse the existing `CHATIFY_PRODUCTION_SMOKE` and `CHATIFY_SMOKE_*` env contract.
- D-11: Use backend API login to seed authenticated browser contexts.
- D-12: Never write raw credentials, cookies, tokens, full emails, request bodies, or private payloads.
- D-28: Collect filtered network, console, API-origin, socket-origin, and response observations, not full HAR.
- D-31: Add `test:e2e:prod`.
- D-32: Missing env, invalid URLs, failed auth, unreachable app, CORS/socket failures, and leaked secrets are blockers.
- D-34: Do not claim functional readiness unless the gate passes with no blocker-grade failures.
</decision_traceability>

<tasks>

<task type="tdd">
  <name>Task 1: Add Phase 14 production environment contract</name>
  <files>Frontend/Chatify/e2e/pages/productionSmoke.ts, Frontend/Chatify/e2e/pages/phase14ProductionAcceptance.ts, Frontend/Chatify/e2e/production-smoke-config.spec.ts</files>
  <read_first>Frontend/Chatify/e2e/pages/productionSmoke.ts, Frontend/Chatify/e2e/production-smoke-config.spec.ts, .planning/phases/14-production-live-acceptance-gate/14-CONTEXT.md</read_first>
  <action>Extend the existing production smoke helper or add a Phase 14 helper that validates `CHATIFY_PRODUCTION_SMOKE=1`, `CHATIFY_PROD_FRONTEND_URL`, `CHATIFY_PROD_BACKEND_URL`, and both smoke account credentials. Reject local origins, malformed URLs, missing env vars, and silent defaults as production readiness inputs. Preserve the current Phase 10 and 10.1 helper exports and artifact behavior.</action>
  <verify>cd Frontend/Chatify; npm run test:e2e:prod -- --grep "production smoke config"</verify>
  <acceptance_criteria>
    - Missing env vars produce a named blocked/skipped reason without printing values.
    - Localhost, loopback, empty, and malformed URLs are rejected for Phase 14 production acceptance.
    - Redacted account labels are available for reporting, but full emails and passwords are never returned to artifact writers.
    - Existing Phase 10 and 10.1 production specs still import their helper functions without changes or failures.
  </acceptance_criteria>
  <done>Phase 14 has an explicit production-only contract.</done>
</task>

<task type="tdd">
  <name>Task 2: Add production Playwright config and npm script</name>
  <files>Frontend/Chatify/playwright.production.config.ts, Frontend/Chatify/package.json</files>
  <read_first>Frontend/Chatify/playwright.config.ts, Frontend/Chatify/package.json, 14-RESEARCH.md</read_first>
  <action>Create `playwright.production.config.ts` that uses the configured deployed frontend URL, has no `webServer`, enables fake media device flags, and configures artifact-friendly output for traces/screenshots/videos. Add `test:e2e:prod` to run Playwright with the production config.</action>
  <verify>cd Frontend/Chatify; npm run test:e2e:prod -- --list</verify>
  <acceptance_criteria>
    - The production config does not start Vite or any local server.
    - Local Playwright config remains unchanged for local UI tests.
    - The command can list tests without production credentials.
    - Fake microphone/camera flags are present for later call/video acceptance.
    - Output directories are distinct from local smoke artifacts where practical.
  </acceptance_criteria>
  <done>Phase 14 can be launched with one focused production E2E command.</done>
</task>

<task type="tdd">
  <name>Task 3: Add Phase 14 artifact writer, blocker aggregator, and redaction scan</name>
  <files>Frontend/Chatify/e2e/pages/phase14ProductionAcceptance.ts, Frontend/Chatify/e2e/pages/productionSmoke.ts, .planning/phases/14-production-live-acceptance-gate/14-LIVE-ACCEPTANCE.md</files>
  <read_first>Frontend/Chatify/e2e/pages/productionSmoke.ts, .planning/phases/10-production-messenger-reality-audit-and-fixture-removal/10-CONTEXT.md, .planning/phases/10.1-production-message-delivery-reliability-repair/10.1-CONTEXT.md</read_first>
  <action>Add a Phase 14 report writer that records command, configured origins, optional deployed commit ids, redacted accounts, pass/fail rows, blocker rows, evidence paths, and remaining risks. Add a blocker aggregator that collects findings and fails the suite at the end when setup permits. Add secret redaction helpers and a static denylist scaffold without yet requiring the full live workflow.</action>
  <verify>cd Frontend/Chatify; npm run test:e2e:prod -- --grep "production smoke config"</verify>
  <acceptance_criteria>
    - `14-LIVE-ACCEPTANCE.md` is created or updated with a blocked skeleton when production setup is missing.
    - Artifact rows contain only redacted account labels and configured origins.
    - Missing-env rows list variable names, not values.
    - The helper never writes raw cookies, tokens, passwords, full emails, request bodies, or private payloads.
    - Static denylist includes known demo values and supports current-run marker allowlisting.
  </acceptance_criteria>
  <done>Phase 14 has a durable and sanitized evidence channel.</done>
</task>

<task type="verification">
  <name>Task 4: Verify harness quality gates</name>
  <files>Frontend/Chatify/e2e/pages/productionSmoke.ts, Frontend/Chatify/e2e/pages/phase14ProductionAcceptance.ts, Frontend/Chatify/e2e/production-smoke-config.spec.ts, Frontend/Chatify/playwright.production.config.ts, Frontend/Chatify/package.json</files>
  <read_first>Frontend/Chatify/package.json, Frontend/Chatify/e2e/production-smoke-config.spec.ts</read_first>
  <action>Run the no-env production config tests, frontend tests, lint, build, and a targeted artifact redaction scan. Fix harness-only issues before moving to live workflow implementation.</action>
  <verify>cd Frontend/Chatify; npm run test:e2e:prod -- --grep "production smoke config"; npm test; npm run lint; npm run build</verify>
  <acceptance_criteria>
    - No-env production gate behavior is blocked/skipped, not false-pass.
    - Frontend unit tests pass.
    - Frontend lint passes.
    - Frontend build passes.
    - `14-LIVE-ACCEPTANCE.md` contains no obvious credentials, tokens, cookies, or full account identifiers.
  </acceptance_criteria>
  <done>Harness is ready for live behavior checks.</done>
</task>

</tasks>

<verification>
- `cd Frontend/Chatify; npm run test:e2e:prod -- --list`
- `cd Frontend/Chatify; npm run test:e2e:prod -- --grep "production smoke config"`
- `cd Frontend/Chatify; npm test`
- `cd Frontend/Chatify; npm run lint`
- `cd Frontend/Chatify; npm run build`
- `rg -n "CHATIFY_SMOKE_USER|password|accessToken|refreshToken|Set-Cookie|Cookie:|Authorization:" .planning/phases/14-production-live-acceptance-gate/14-LIVE-ACCEPTANCE.md`
</verification>

<success_criteria>
- Phase 14 has a production-only Playwright runtime and script.
- Missing setup produces a clean blocked artifact.
- The helper/reporting layer redacts secrets by design.
- Older production smoke helpers remain usable.
- No local fixture or local server path can count as production readiness.
</success_criteria>
