---
phase: 26
plan: 26-01
type: implementation
wave: 1
depends_on: []
autonomous: true
requirements: [SEC-01, SEC-02, SEC-04, TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, PROD-04]
files_modified:
  - .github/workflows/security-and-test-foundation.yml
---

<objective>
Upgrade the existing GitHub Actions workflow so CI runs backend, frontend, operations, release evidence, browser smoke config, and aggregate quality gates.
</objective>

<tasks>

1. Expand backend/frontend jobs.
   - Action: Add high-severity audit thresholds and frontend tests.
   - Verify: Workflow YAML validates.
   - Acceptance: Backend and frontend local equivalents pass.

2. Add operations/evidence job.
   - Action: Run `ops:check`, generate Phase 25 evidence, upload artifact.
   - Verify: Missing production evidence warns unless release variable is set.
   - Acceptance: PRs do not hide release blockers.

3. Add browser smoke config and aggregate gate.
   - Action: Run production smoke config Playwright spec and aggregate required jobs.
   - Verify: Local Playwright command passes.
   - Acceptance: Required quality gate fails if any required job fails.

</tasks>

<verification>
- YAML parser validation.
- Local audit/test/lint/build/smoke equivalents.
</verification>

<success_criteria>
- CI covers the meaningful local quality gates.
- Release evidence is visible and optionally blocking.
</success_criteria>
